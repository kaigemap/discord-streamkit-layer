# Reference Assets

This directory contains legacy CSS kept as implementation reference.

These files are not used by the current app, Vite build, or GitHub Pages output.
They are preserved here to compare against older hand-authored behavior when
changing the generator or StreamKit selectors.

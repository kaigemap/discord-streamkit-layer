{
  "name": "discord-streamkit-layer",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/build-offline.js",
    "build:pages": "vite build",
    "build:offline": "node scripts/build-offline.js",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d docs",
    "bundle-offline": "npm run build:offline"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.0.0"
  }
}
